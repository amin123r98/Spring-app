package com.example.onlinelearning.model;

public enum LessonType {
    TEXT,
    VIDEO,
    EXTERNAL_RESOURCE, // Ссылка на внешний ресурс
    FILE // Загружаемый файл (например, PDF, презентация)
    // Можно добавить другие типы по необходимости
}